import { AppRegistry } from 'react-native';
import App from '../Sanjog/App'; // Adjust the path based on your project structure
import { name as appName } from '../Sanjog/app.json';

AppRegistry.registerComponent(appName, () => App);
